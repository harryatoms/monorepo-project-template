# Staging Platform

Shared staging platform notes for the boilerplate. App-specific Terraform roots live in `infra/environments/staging/apps/`.

Use `example-monorepo` values as greenfield placeholders and replace them before applying infrastructure for a real project.
