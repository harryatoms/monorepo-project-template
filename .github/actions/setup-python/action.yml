name: Python Setup
description: Configure Python (version pinned to mise.toml)

runs:
  using: composite
  steps:
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.13"
